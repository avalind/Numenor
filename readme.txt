Numenor - Just another n-body simulation.

It uses a verlet integrator, particles that moves away
from the screen, (ie outside of (0, 0) to (640, 480)) are removed.

The code should be mostly self-explainatory.
